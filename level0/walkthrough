Test de strings, ltrace, strace sur le binaire -> Aucun resultat
Lancement du binaire sans arguments -> SegV
Lancement du binaire avec -> "No !"
Ouverture avec gdb et reecriture du source du binaire en C
Test de modifier le retour des fonctions getuid et getgid avec infos du level1 (2030) -> Ne fonctionne pas
